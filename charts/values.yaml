environment: development

apphost: k8s

label:
  name: pocback

container:
  name: backend
  pullPolicy: Always
  image: index.docker.io/iqbaleet/azurepoc
  tag: latest
  port: 80
replicas: 1

service:
  port: 8888
  type: LoadBalancer
